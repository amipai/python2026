import pygame

pygame.init()

screen = pygame.display.set_mode((400,400))

pygame.draw.rect(screen, (0,255,0), (1, 5, 6, 7))


for event in pygame.event.get():
    if event.type == pygame.QUIT():
        running=False


pygame.quit()